{
  "name": "@liquidity-arena/cloudflare-keeper-scheduler",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.9"
  },
  "scripts": {
    "test": "node --test src/*.test.mjs",
    "check": "wrangler deploy --dry-run --config wrangler.jsonc"
  },
  "devDependencies": {
    "wrangler": "4.125.0"
  }
}
